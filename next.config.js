/** @type {import('next').NextConfig} */
const nextConfig = {
    // for external images
    images: {
        domains: [
            "images.pexels.com",
            "i.ytimg.com",
            "spng.pngfind.com",
        ]
    }
}

module.exports = nextConfig
